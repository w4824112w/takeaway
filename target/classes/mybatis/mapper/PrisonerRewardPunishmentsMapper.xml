<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper
	namespace="com.gkyt.ywgk.modular.dao.mapper.PrisonerRewardPunishmentsMapper">
	<resultMap id="BaseResultMap"
		type="com.gkyt.ywgk.modular.dao.model.PrisonerRewardPunishments">
		<id column="id" property="id" jdbcType="INTEGER" />
		<result column="prisoner_id" property="prisonerId" jdbcType="INTEGER" />
		<result column="datayear" property="datayear" jdbcType="VARCHAR" />
		<result column="ndry" property="ndry" jdbcType="VARCHAR" />
		<result column="rp_type" property="rpType" jdbcType="VARCHAR" />
		<result column="created_at" property="createdAt" jdbcType="DATE" />
		<result column="updated_at" property="updatedAt" jdbcType="DATE" />
	</resultMap>

	<sql id="Base_Column_List">
		id,prisoner_id,datayear,ndry,rp_type,created_at,updated_at
	</sql>

	<select id="getById" parameterType="java.lang.String" resultMap="BaseResultMap">
		select
		<include refid="Base_Column_List" />
		from prisoner_reward_punishments
		where id = #{id,jdbcType=VARCHAR}
	</select>

	<select id="getAll" resultMap="BaseResultMap">
		select
		<include refid="Base_Column_List" />
		from prisoner_reward_punishments order by id desc
	</select>

	<insert id="save"
		parameterType="com.gkyt.ywgk.modular.dao.model.PrisonerRewardPunishments">
		<selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
			SELECT
			LAST_INSERT_ID()
		</selectKey>
		insert into prisoner_reward_punishments (
		prisoner_id,
		datayear,
		ndry,
		rp_type,
		created_at,
		updated_at
		)
		values
		(#{prisonerId,jdbcType=INTEGER},#{datayear,jdbcType=VARCHAR},
		#{ndry,jdbcType=VARCHAR},#{rpType,jdbcType=VARCHAR},
		#{createdAt,jdbcType=TIMESTAMP},#{updatedAt,jdbcType=TIMESTAMP}
		)
	</insert>

	<select id="getPrisonerRewardPunishmentsForApi" parameterType="map"
		resultType="map">
		SELECT
		pt.id,
		pt.prisoner_id AS prisonerId,
		pt.datayear,
		pt.ndry,
		pt.rp_type AS rpType,
		pt.created_at AS createdAt,
		pt.updated_at AS updatedAt,
		pr.prisoner_number AS prisonerNumber,
		pr.jail_id AS jailId
		FROM
		prisoner_reward_punishments pt
		LEFT JOIN prisoners pr ON pt.prisoner_id = pr.id
		WHERE
		1 = 1
		<if test="prisonerNumber != null and prisonerNumber != ''">
			and pr.prisoner_number =
			#{prisonerNumber,jdbcType=VARCHAR}
		</if>
		<if test="jailId != null and jailId != ''">
			and pr.jail_id = #{jailId,jdbcType=INTEGER}
		</if>
	</select>

</mapper>