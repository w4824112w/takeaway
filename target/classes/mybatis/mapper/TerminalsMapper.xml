<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gkyt.ywgk.modular.dao.mapper.TerminalsMapper">
		<resultMap id="BaseResultMap" type="com.gkyt.ywgk.modular.dao.model.Terminals">
		<id column="id" property="id" jdbcType="INTEGER" />
		<result column="jail_id" property="jailId"
			jdbcType="VARCHAR" />
		<result column="terminal_number" property="terminalNumber" jdbcType="VARCHAR" />
		<result column="updated_at" property="updatedAt" jdbcType="DATE" />
		<result column="created_at" property="createdAt" jdbcType="DATE" />
		<result column="room_number" property="roomNumber" jdbcType="VARCHAR" />
		<result column="host_password" property="hostPassword" jdbcType="VARCHAR" />
		<result column="metting_password" property="mettingPassword"
			jdbcType="VARCHAR" />
	</resultMap>
	<select id="findPage" parameterType="map"
		resultMap="BaseResultMap">
		select 
		t.id, 
		t.jail_id,
		t.terminal_number,
		t.room_number,
		t.host_password,
		t.metting_password,
		t.created_at,
		t.updated_at
		from  terminals t
		where 1=1
		<if test="jailId != null and jailId != ''">
			and t.jail_id = #{jailId,jdbcType=INTEGER}
		</if>
	</select>
	
	<insert id="insert" parameterType="map">
		<selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
			SELECT
			LAST_INSERT_ID()
		</selectKey>
		insert into terminals (
		jail_id,
		created_at,
		updated_at,
		terminal_number,
		room_number,
		host_password,
		metting_password
		)
		values
		(
		#{jailId,jdbcType=INTEGER},
		#{createdAt,jdbcType=TIMESTAMP},
		#{updatedAt,jdbcType=TIMESTAMP},
		#{terminalNumber,jdbcType=VARCHAR},
		#{roomNumber,jdbcType=VARCHAR},
		#{hostPassword,jdbcType=VARCHAR},
		#{mettingPassword,jdbcType=VARCHAR}
		)
	</insert>
	
	<update id="update" parameterType="map">
		update terminals
		set
		terminal_number =
		#{terminalNumber,jdbcType=VARCHAR},
		room_number =
		#{roomNumber,jdbcType=VARCHAR},
		metting_password =
		#{mettingPassword,jdbcType=VARCHAR},
		host_password =
		#{hostPassword,jdbcType=VARCHAR},
		updated_at =
		#{updatedAt,jdbcType=TIMESTAMP}
		where id =
		#{id,jdbcType=INTEGER}
	</update>
	
	<select id="exist" parameterType="map" resultType="java.lang.Integer">
		select count(1) from terminals
		where 1=1
		<if test="terminalNumber != null and terminalNumber != ''">
			and terminal_number = #{terminalNumber,jdbcType=INTEGER}
		</if>
		<if test="id != null and id != ''">
			and id != #{id,jdbcType=INTEGER}
		</if>
	</select>
</mapper>