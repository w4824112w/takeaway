<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gkyt.ywgk.modular.dao.mapper.FamiliesLogMapper">
	<select id="findPage" parameterType="map" resultType="map">
		select id,family_id as familyId,
		prisoner_id as prisonerId,
		created_at as createdAt
		from family_logs where 1=1
		<if test="prisonerId != null and prisonerId != ''">
			and prisoner_id =#{prisonerId,jdbcType=INTEGER}
		</if>
		<if test="familyId != null and familyId != ''">
			and family_id =#{familyId,jdbcType=INTEGER}
		</if>
		order by created_at desc
	</select>

	<insert id="insert" parameterType="map">
		<selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
			SELECT
			LAST_INSERT_ID()
		</selectKey>
		insert into family_logs (
		prisoner_id,
		family_id,
		content,
		created_at
		)
		values
		(#{prisonerId,jdbcType=INTEGER}, #{familyId,jdbcType=INTEGER},
		#{content,jdbcType=VARCHAR},#{createdAt,jdbcType=TIMESTAMP}
		)
	</insert>
</mapper>